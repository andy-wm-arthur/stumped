/**
* @author Andy Arthur
* @author Ramya Puliadi
*/

class Network( val layers: List[funMatrix], val biases: List[funVector]) {

	def forwardProp( dataPnt: funVector, sigmoid: Double => Double): funVector = {
		fprop_r( layers: List[funMatrix, biases: List[Double] activations: funVector, sigmoid: Double => Double): funVector = {
			layers match {
				case Nil => activations
			}
			
		}
		
	}

}